#include <bits/stdc++.h>
using namespace std;

int main()
{
    int m, len, s1, s2;
    vector <pair <int, int> > v;
    while(len--)
    {   
        cin >> s1 >> s2;
        v.push_back({s1, s2});
    }
    sort(v.begin(), v.end());
    pair <int, int> p;
    p = v[0];
    for(int i)

}