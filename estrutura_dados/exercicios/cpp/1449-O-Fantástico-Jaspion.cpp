#include <bits/stdc++.h>
using namespace std;

int main()
{
    int dict, music;
    cin >> dict >> music;

    map < string, string > m;
    string key, value;
    while(dict)
    {
        cin >> key >> value;
        m[ key ] = value;
        dict--;
    }
    string l;
    while(music)
    {
        cin >> l;
        
    }
}